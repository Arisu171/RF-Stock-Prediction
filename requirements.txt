# =====================================================================
# Phụ thuộc của dự án
# =====================================================================
# Tầng lõi src/libraries/ được viết bằng Python thuần, KHÔNG dùng bất kỳ
# gói nào dưới đây. Các gói chỉ phục vụ vào/ra dữ liệu, vẽ đồ thị,
# chạy notebook và đối chiếu kết quả với scikit-learn.
# =====================================================================

# ── Nhóm chạy máy chủ web — nguồn duy nhất, ảnh Docker chỉ cần nhóm này
-r requirements-api.txt

# ── Vẽ đồ thị ────────────────────────────────────────────────────────
matplotlib>=3.7.0

# ── Môi trường notebook ──────────────────────────────────────────────
jupyter>=1.0.0
notebook>=7.0.0
ipykernel>=6.0.0

# ── Đối chiếu baseline ───────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.1.0
scikit-learn>=1.4.0

# ── Kiểm thử ─────────────────────────────────────────────────────────
pytest>=8.0.0
